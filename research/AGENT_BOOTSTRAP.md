# Agent Bootstrap

Use local Ollama as primary reasoning.

Checklist:

1. Confirm Ollama endpoint is available at `http://localhost:11434`.
2. Add/refresh research notes with `research-add`.
3. Run `help-agent --save` to refresh onboarding guide.
4. Execute `run --instruction ...` for autonomous output.
